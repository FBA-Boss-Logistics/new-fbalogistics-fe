version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fbalogistics_fe
    ports:
      - "3000:80"
    env_file:
      - .env
    networks:
      - logistics_network

networks:
  logistics_network:
